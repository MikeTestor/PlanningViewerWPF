<UserControl x:Class="PlanningViewerWPF.WPF.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PlanningViewerWPF.WPF.Views" 
             xmlns:guicontrols="clr-namespace:PlanningViewerWPF.WPF.GuiControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <local:NavigationView Grid.Row="0"/>
        <local:GridView Grid.Row="1"/> 
        <local:SearchAndDisplayView Grid.Row="2"/>
        <guicontrols:BottomControl Grid.Row="3"/>
    </Grid>
</UserControl>
